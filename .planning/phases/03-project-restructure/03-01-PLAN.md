---
phase: 03-project-restructure
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REST-01
  - REST-02

must_haves:
  truths:
    - "Source code is in src/lib/ directory"
    - "Tests directory exists (test/)"
    - "Examples exist in src/examples/"
    - "Demo at src/index.html exists"
    - "Vite entry point configured correctly"
    - "Entry point re-exports from src/lib/"
  artifacts:
    - path: "src/lib/heatmap.js"
      provides: "Main library source"
    - path: "src/heatmap.css"
      provides: "Library styles"
    - path: "src/index.html"
      provides: "Demo page"
    - path: "src/examples/basic.js"
      provides: "Example usage"
    - path: "test/"
      provides: "Test directory"
    - path: "index.js"
      provides: "Entry point re-exporting from src/lib"
  key_links:
    - from: "index.js"
      to: "src/lib/heatmap.js"
      via: "export statement"
      pattern: "from './src/lib/heatmap'"
    - from: "vite.config.mjs"
      to: "src/lib/heatmap.js"
      via: "lib.entry config"
      pattern: "src/lib/heatmap.js"
---

<objective>
Verify project structure matches d3-flame-graph layout. The source code was moved to src/lib/ during Phase 1; this plan confirms all requirements are met.
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-project-restructure/03-CONTEXT.md
@.planning/phases/03-project-restructure/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Verify source code in src/lib/ directory (REST-01)</name>
  <files></files>
  <action>Verify the following exist and are correct:
- src/lib/heatmap.js: Main library source with D3 getter-setter pattern
- src/heatmap.css: Styles imported in source
- index.js: Entry point re-exporting from './src/lib/heatmap'

Run verification commands:
- ls src/lib/heatmap.js
- cat index.js (should show: export { default } from './src/lib/heatmap')</action>
  <verify>
    <automated>ls src/lib/heatmap.js src/heatmap.css index.js && grep -q "from './src/lib/heatmap'" index.js</automated>
  </verify>
  <done>REST-01 verified: src/lib/heatmap.js exists and exports main heatmap function</done>
</task>

<task type="auto">
  <name>Verify test and examples directories exist (REST-02)</name>
  <files></files>
  <action>Verify the following directories and files exist:
- test/: Test directory (can be empty, ready for Phase 2)
- src/examples/basic.js: Example usage file

Run verification commands:
- ls -d test/
- ls src/examples/basic.js</action>
  <verify>
    <automated>ls -d test/ && ls src/examples/basic.js</automated>
  </verify>
  <done>REST-02 verified: test/ directory exists, src/examples/basic.js exists</done>
</task>

<task type="auto">
  <name>Verify demo and Vite configuration (REST-01, REST-02)</name>
  <files></files>
  <action>Verify:
- src/index.html: Demo page exists
- vite.config.mjs: Entry point points to src/lib/heatmap.js

Run verification commands:
- ls src/index.html
- grep -q 'src/lib/heatmap.js' vite.config.mjs</action>
  <verify>
    <automated>ls src/index.html && grep -q "src/lib/heatmap.js" vite.config.mjs</automated>
  </verify>
  <done>Demonstrated: src/index.html exists, Vite configured to build from src/lib/</done>
</task>

</tasks>

<verification>
Run all verification commands:
```bash
ls src/lib/heatmap.js src/heatmap.css src/index.html src/examples/basic.js index.js && \
ls -d test/ && \
grep -q "from './src/lib/heatmap'" index.js && \
grep -q "src/lib/heatmap.js" vite.config.mjs
```
All commands should pass without errors.
</verification>

<success_criteria>
- src/lib/heatmap.js exists
- src/heatmap.css exists
- src/index.html exists
- src/examples/basic.js exists
- test/ directory exists
- index.js re-exports from src/lib/heatmap
- vite.config.mjs entry points to src/lib/heatmap.js
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-restructure/03-01-SUMMARY.md`
</output>