---
phase: 02-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [test/heatmap-color-test.js, test/heatmap-scale-test.js, test/heatmap-highlight-test.js, test/heatmap-accessibility-test.js]
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04]
user_setup: []

must_haves:
  truths:
    - "Color scale calculations produce correct output for given inputs"
    - "X/Y scale calculations produce correct output for given inputs"
    - "Highlight calculation logic identifies correct cells to highlight"
    - "Accessibility tests verify SVG elements have proper ARIA attributes"
  artifacts:
    - path: "test/heatmap-color-test.js"
      provides: "Unit tests for color scale calculations (TEST-02)"
      contains: "describe.*color"
    - path: "test/heatmap-scale-test.js"
      provides: "Unit tests for x/y scale calculations (TEST-02)"
      contains: "describe.*scale"
    - path: "test/heatmap-highlight-test.js"
      provides: "Unit tests for highlight calculation logic (TEST-03)"
      contains: "describe.*highlight"
    - path: "test/heatmap-accessibility-test.js"
      provides: "Accessibility tests using axe-core (TEST-04)"
      contains: "axe.*violation"
  key_links:
    - from: "test/heatmap-color-test.js"
      to: "d3-scale"
      via: "import"
      pattern: "import.*scaleLinear.*d3-scale"
    - from: "test/heatmap-accessibility-test.js"
      to: "vitest-axe"
      via: "import"
      pattern: "import.*vitest-axe"
---

<objective>
Create comprehensive unit tests covering color scale, x/y scale, highlight logic, and accessibility.

Purpose: Establish quality baseline with tests that verify correct behavior for all key calculations.

Output: Four test files that all pass when run via `npm test`.
</objective>

<context>
@.planning/phases/02-test-infrastructure/02-01-SUMMARY.md
@.planning/phases/02-test-infrastructure/02-RESEARCH.md

# Source code being tested
src/lib/heatmap.js - Main heatmap implementation

# Test patterns from research
- D3 scale testing pattern: Create scale, call with input, expect output
- Accessibility testing: Use vitest-axe with axe() function and toHaveNoViolations()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create heatmap-color-test.js</name>
  <files>test/heatmap-color-test.js</files>
  <action>
Create test file with Vitest tests for color scale calculations:

Import from d3-scale: scaleLinear, scaleSequential
Import from d3-scale-chromatic: interpolateReds, interpolateBlues

Tests to include:
1. Linear color scale with 2-point domain/range - verify midpoints interpolate correctly
2. Linear color scale with 3-point domain/range - verify multi-stop gradient
3. Sequential color scale using d3-scale-chromatic - verify valid RGB output
4. Color scale domain validation - verify domain affects output range

Use expect with toBe for exact matches where possible, toMatch for RGB patterns.

Follow AGENTS.md style: 2-space indentation, single quotes, no semicolons.
  </action>
  <verify>
    <automated>vitest run test/heatmap-color-test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All color scale tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create heatmap-scale-test.js</name>
  <files>test/heatmap-scale-test.js</files>
  <action>
Create test file with Vitest tests for x/y scale calculations:

Import from d3-scale: scaleBand, scaleLinear

Tests to include:
1. Band scale for x-axis - verify domain maps to range positions
2. Band scale bandwidth calculation - verify cell width
3. Band scale padding - verify gaps between cells
4. Linear scale for continuous data - verify interpolation
5. Band scale domain validation - verify categories correctly mapped

Use expect with toBeCloseTo for floating point comparisons.

Follow AGENTS.md style: 2-space indentation, single quotes, no semicolons.
  </action>
  <verify>
    <automated>vitest run test/heatmap-scale-test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All scale tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create heatmap-highlight-test.js</name>
  <files>test/heatmap-highlight-test.js</files>
  <action>
Create test file with Vitest tests for highlight calculation logic:

Tests to include:
1. Highlight cells above threshold - filter data by value >= threshold
2. Highlight cells below threshold - filter data by value < threshold
3. Highlight with custom predicate - verify flexible filtering
4. Highlight multiple conditions - verify AND/OR logic
5. Highlight with empty result - verify empty array handling
6. Highlight preserves original data - verify no mutation

The heatmap library likely has a highlightValue getter/setter. Test the highlight calculation logic independently.

Follow AGENTS.md style: 2-space indentation, single quotes, no semicolons.
  </action>
  <verify>
    <automated>vitest run test/heatmap-highlight-test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All highlight tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Create heatmap-accessibility-test.js</name>
  <files>test/heatmap-accessibility-test.js</files>
  <action>
Create test file with Vitest tests for accessibility using axe-core:

Setup:
- Import 'vitest-axe/extend-expect' in the test file
- Import axe from 'vitest-axe'
- Extend expect with toHaveNoViolations()

Tests to include:
1. SVG with role="graphics-document" and aria-label - should have no violations
2. SVG with title element - should have no violations
3. SVG with proper structure - should have no violations
4. Missing aria-label - should detect violation (test the test itself)

Note: This tests that when accessibility attributes ARE provided, there are no violations.
The actual heatmap implementation will add these in a later phase.

Follow AGENTS.md style: 2-space indentation, single quotes, no semicolons.
  </action>
  <verify>
    <automated>vitest run test/heatmap-accessibility-test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All accessibility tests pass</done>
</task>

</tasks>

<verification>
- Run `npm test` - all 4 test files should execute
- Verify all tests pass (0 failures)
- Check that jsdom environment is being used
</verification>

<success_criteria>
- All tests in test/heatmap-*-test.js pass
- npm test runs without errors
- TEST-02, TEST-03, TEST-04 requirements are covered
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure/02-02-SUMMARY.md`
</output>