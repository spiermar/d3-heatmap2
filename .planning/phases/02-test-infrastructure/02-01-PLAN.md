---
phase: 02-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vitest.config.js]
autonomous: true
requirements: [TEST-01]
user_setup: []

must_haves:
  truths:
    - "npm test runs Vitest test suite with jsdom environment"
    - "Vitest configuration file exists at project root"
    - "Test script updated to use vitest instead of tape"
  artifacts:
    - path: "package.json"
      provides: "Updated test script pointing to vitest"
      contains: "\"test\": \"vitest run\""
    - path: "vitest.config.js"
      provides: "Vitest configuration with jsdom environment"
      contains: "environment: 'jsdom'"
  key_links:
    - from: "package.json"
      to: "vitest.config.js"
      via: "test script"
      pattern: "vitest run"
---

<objective>
Set up Vitest test infrastructure with jsdom environment, replacing the current Tape-based test setup.

Purpose: Provides the foundation for comprehensive unit testing with modern tooling.

Output: Working test infrastructure with `npm test` executing Vitest.
</objective>

<context>
@.planning/phases/01-build-system-modernization/01-01-SUMMARY.md
@.planning/phases/02-test-infrastructure/02-CONTEXT.md
@.planning/phases/02-test-infrastructure/02-RESEARCH.md

# Project source that tests will validate
src/lib/heatmap.js - Main heatmap implementation (D3 getter-setter pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and related dependencies</name>
  <files>package.json</files>
  <action>
Install the following devDependencies:
- vitest (latest) - Test runner
- jsdom (latest) - Browser environment for DOM testing
- vitest-axe (latest) - Accessibility testing matchers
- axe-core (latest) - Core accessibility engine

Run: npm install --save-dev vitest jsdom vitest-axe axe-core
  </action>
  <verify>
    <automated>npm list vitest jsdom vitest-axe axe-core 2>/dev/null | grep -E "vitest|jsdom|vitest-axe|axe-core"</automated>
  </verify>
  <done>Dependencies installed and visible in npm list</done>
</task>

<task type="auto">
  <name>Task 2: Create vitest.config.js with jsdom environment</name>
  <files>vitest.config.js</files>
  <action>
Create vitest.config.js at project root with:
- environment: 'jsdom' (required for DOM testing)
- include: ['test/**/*-test.js'] (match test file pattern)
- globals: true (enable global expect/it/describe)
- testTimeout: 5000, hookTimeout: 5000
- Resolve aliases for D3 modules if needed

Use the pattern from 02-RESEARCH.md - use defineConfig from vitest/config.
  </action>
  <verify>
    <automated>node -e "import('./vitest.config.js').then(m => console.log('Config loads:', Object.keys(m.default.test || {})))"</automated>
  </verify>
  <done>Config file exists and loads without errors</done>
</task>

<task type="auto">
  <name>Task 3: Update package.json test script</name>
  <files>package.json</files>
  <action>
Change test script from:
  "test": "tape 'test/**/*-test.js'"
To:
  "test": "vitest run"

Remove the "prepare": "npm run test" script if present (tape pretest hook is no longer needed).
  </action>
  <verify>
    <automated>npm test -- --version 2>&1 | head -3</automated>
  </verify>
  <done>npm test runs Vitest (shows Vitest version)</done>
</task>

</tasks>

<verification>
- Run `npm test` - should execute Vitest test suite
- Verify jsdom environment is active: check vitest output shows jsdom
- No test files exist yet, so Vitest should report "0 tests" but run without errors
</verification>

<success_criteria>
- `npm test` executes Vitest without errors
- vitest.config.js exists with jsdom environment
- Test script uses "vitest run" command
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure/02-01-SUMMARY.md`
</output>