---
phase: 04-upgrade-all-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements: []
nyquist_compliant: true
must_haves:
  truths:
    - "Build passes with upgraded D3 v3/v4 modules"
    - "Lint passes with ESLint v10"
    - "Deprecated packages removed from dependencies"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency declarations"
      contains: "d3-array"
      min_version: "3.2.4"
  key_links: []
user_setup: []
---

<objective>
Upgrade all npm dependencies to latest versions, completing the modernization path from Phase 1 (Vite), Phase 2 (Vitest), Phase 3 (Restructure).

Purpose: Update D3 modules from v1 to v3/v4, upgrade ESLint, and remove deprecated packages while maintaining full build/lint compatibility.

Output: Updated package.json with latest dependencies, build and lint verified
</objective>

<execution_context>
@/Users/mspier/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/mspier/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-upgrade-all-dependencies/04-CONTEXT.md
@.planning/phases/04-upgrade-all-dependencies/04-RESEARCH.md
@.planning/phases/04-upgrade-all-dependencies/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade D3 modules and remove deprecated packages</name>
  <files>package.json</files>
  <action>
    Execute the following npm commands to upgrade dependencies:

    1. Install upgraded D3 modules:
    ```bash
    npm install d3-array@^3.2.4 d3-axis@^3.0.0 d3-format@^3.1.2 d3-scale@^4.0.2 d3-selection@^3.0.0
    ```

    2. Upgrade yargs to latest for security:
    ```bash
    npm install yargs@^17.7.0
    ```

    3. Remove deprecated packages:
    ```bash
    npm uninstall vite-plugin-lib-inject-css uglify-es @eslint/js
    ```

    4. Ensure ESLint v10 is installed:
    ```bash
    npm install eslint@^10.0.0
    ```

    The research confirms all D3 v3/v4 APIs used in src/lib/heatmap.js are backward compatible.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>package.json updated with D3 v3/v4 modules, deprecated packages removed, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Verify lint passes after upgrade</name>
  <files>package.json</files>
  <action>
    Run lint to verify ESLint works correctly with upgraded dependencies:
    ```bash
    npm run lint
    ```

    Fix any lint errors if they appear (should not require code changes since the APIs are compatible).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>npm run lint passes without errors</done>
</task>

</tasks>

<verification>
After plan completion:
- npm run build completes without errors
- npm run lint passes without errors
- package.json shows d3-array@^3.2.4, d3-axis@^3.0.0, d3-format@^3.1.2, d3-scale@^4.0.2, d3-selection@^3.0.0, yargs@^17.7.0, eslint@^10.0.0
- package.json no longer contains vite-plugin-lib-inject-css, uglify-es, @eslint/js
</verification>

<success_criteria>
- All D3 modules upgraded from v1 to v3/v4
- ESLint upgraded to v10 with flat config
- Deprecated packages (vite-plugin-lib-inject-css, uglify-es, @eslint/js) removed
- yargs upgraded from v11 to v17 for security
- Build passes: `npm run build`
- Lint passes: `npm run lint`
</success_criteria>

<output>
After completion, create `.planning/phases/04-upgrade-all-dependencies/04-01-SUMMARY.md`
</output>